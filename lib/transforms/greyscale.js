'use strict';

// Export just a method that we can call from the bitmap's transform method
module.exports = (bmp) => {

  if( ! bmp.colorArray.length ) throw 'must pass valid bmp object';

  //TODO: alter bmp to make the image greyscale ...

};
